node_modules
dist
.DS_Store
# Derived web-optimized art — regenerated by scripts/optimize-card-art.mjs
# (runs automatically via predev/prebuild). See CLAUDE.md.
web_art
# Smoke-suite screenshots (npm run smoke)
smoke_output
# Card back / logo trial renders (~60 MB of explorations). The chosen design
# ships from card_back/; brand/README.md and its two comparison sheets are
# tracked, so the write-up survives without the rejected concepts.
brand/concepts
# Per-machine Claude Code permission grants
.claude/settings.local.json
